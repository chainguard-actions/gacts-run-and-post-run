# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
# docs: https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions

name: 🧪 Tests

on:
  push:
    branches: [master, main]
    tags-ignore: ['**']
    paths-ignore: ['**.md']
  pull_request:
    paths-ignore: ['**.md']

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

permissions: {}

env: {FORCE_COLOR: 'true'}

jobs:
  gitleaks:
    name: Check for GitLeaks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - {uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4, with: {fetch-depth: 0}}
      - uses: gacts/gitleaks@c9a0338361dc45a01aa7ebaaa5330179f3c62873 # v1

  eslint:
    name: Run code linter
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - {uses: gacts/setup-node-with-cache@624bb72adfed6b190561a8544d80f789135e462d # v1, with: {node-version: 20}}
      - run: npm install
      - run: npm run lint

  dist-built:
    name: Check distributive built state
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      dist-changed: ${{ steps.state.outputs.changed }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - {uses: gacts/setup-node-with-cache@624bb72adfed6b190561a8544d80f789135e462d # v1, with: {node-version: 20}}
      - run: npm install
      - run: npm run build
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with: {name: dist, path: ./dist/, retention-days: 1}
      - id: state
        run: echo "changed=`git diff --diff-filter=ACMUXTR --name-only | grep dist/ > /dev/null && echo 'true' || echo 'false'`" >> $GITHUB_OUTPUT

  commit-and-push-fresh-dist:
    name: Commit and push fresh distributive
    needs: [dist-built]
    if: ${{ needs.dist-built.outputs.dist-changed == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with: {name: dist, path: ./dist/}
      - uses: stefanzweifel/git-auto-commit-action@b863ae1933cb653a53c021fe36dbb774e1fb9403 # v5
        with: {commit_message: Automatic distributive rebuild}

  run-this-action:
    name: Run action
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Run this action
        uses: ./
        with:
          run: echo "First"
              "(can be multiline)"
          post: |
            echo "First post"
            echo "(can run multiply commands)"

      - name: Post only
        uses: ./
        with:
          post: echo "Second post"

      - name: Execute without shell
        uses: ./
        with:
          shell: ''
          run: echo foo
          post: echo bar

      - name: Environment variables
        uses: ./
        env: { ENV_VAR: 'value123' }
        with:
          run: echo "$ENV_VAR" > ./test_env_var.txt
          post: if grep -q value123 ./test_env_var.txt; then echo "Test passed"; else exit 1; fi

      - name: Multiline with line breaks
        uses: ./
        with:
          run: |
            ls \
              -la \
              /tmp /opt \
                /bin
            cat /etc/passwd
          post: echo "Foo"
            echo "bar"
            echo baz \ blah \
              boom

      - name: Change post shell
        uses: ./
        with:
          run: if [ "$0" = "/usr/bin/sh" ]; then echo "Test passed"; else exit 1; fi
          shell: sh
          post: if [ "$0" = "/usr/bin/bash" ]; then echo "Test passed"; else exit 1; fi
          post-shell: bash
