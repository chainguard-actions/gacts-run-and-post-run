name: Test post run command
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          run: echo "main step ran"
          post: echo "post step ran successfully"
      - name: Assert action ran without error
        run: |
          echo "PASS: action completed without error"
